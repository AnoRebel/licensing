{
  "name": "@licensing/fixture-generator",
  "version": "0.2.0-rc.0",
  "private": true,
  "description": "Generates canonical LIC1 token fixtures (valid + tamper variants) consumed by both ports' interop test suites. Not published; lives in the repo solely to keep fixtures/tokens/*/ byte-identical across languages.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "licensing-fixtures": "./bin/licensing-fixtures.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build-all": "bun run ./bin/licensing-fixtures.ts build-all",
    "tamper-all": "bun run ./bin/licensing-fixtures.ts tamper-all"
  },
  "dependencies": {
    "@anorebel/licensing": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "typescript": "6.0.3"
  },
  "engines": {
    "bun": ">=1.3.12"
  }
}
